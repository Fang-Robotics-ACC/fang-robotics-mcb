#include "dimensional_smooth_pid.hpp"

/*
namespace trap
{
    namespace algorithms
    {
        template<typename RawControlUnit, typename RawErrorUnit, typename RawTimeUnit>
        DimensionalSmoothPid<RawControlUnit, RawErrorUnit, RawTimeUnit>::DimensionalSmoothPid(
            const Config& config)
        : m_smoothPid{config.}
        {
        }
    }
}
*/